#include "raylib.h"
#include "stdio.h"
#include "stdlib.h"

int main()
{
    InitWindow(GetScreenWidth()/3,GetScreenHeight()/3, "Jogo da Memoria");
    SetWindowPosition(GetScreenWidth()/2,GetScreenHeight()/2);
    while (1)
    {
        
    }
    CloseWindow();
    
}